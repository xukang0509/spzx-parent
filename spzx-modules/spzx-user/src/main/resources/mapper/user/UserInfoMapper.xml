<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spzx.user.mapper.UserInfoMapper">

    <resultMap type="com.spzx.user.api.domain.UserInfo" id="UserInfoResult" autoMapping="true">
    </resultMap>

    <sql id="selectUserInfoVo">
        select id, username, password, nick_name, phone, avatar, sex, memo, open_id, union_id, last_login_ip, last_login_time, status, create_time, create_by, update_time, update_by, del_flag, remark from user_info
    </sql>

    <select id="selectUserInfoList" parameterType="com.spzx.user.api.domain.UserInfo" resultMap="UserInfoResult">
        <include refid="selectUserInfoVo"/>
        <where>
            del_flag = 0
            <if test="searchValue != null and searchValue != ''">
                and username like concat('%', #{searchValue}, '%')
                or nick_name like concat('%', #{searchValue}, '%')
                or phone like concat('%', #{searchValue}, '%')
            </if>
            <if test="startTime != null and endTime != null">
                and create_time  &gt;= #{startTime}
                and create_time &lt;= #{endTime}
            </if>
        </where>
    </select>

</mapper>
